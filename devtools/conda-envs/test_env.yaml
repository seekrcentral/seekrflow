name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy=2.2
  - attrs
  - cattrs
  - rdkit
  - parmed
  - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - mdtraj
    - parsl


name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy=2.2
  - attrs
  - cattrs
  - rdkit
  - parmed
  - openmm=8.1
  - seekr2_openmm_plugin

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - mdtraj
    - parsl


name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - attrs
  - cattrs
  - rdkit
  - parmed

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - mdtraj
    - parsl


name: test
channels:

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy=2.3
  - attrs
  - cattrs
  - rdkit
  - parmed

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - mdtraj
    - parsl

